%td{colspan: 3}
  = bootstrap_form_for(@seat, layout: :horizontal, label_col: "col-lg-1 col-md-2 offset-lg-3 offset-md-1", control_col: "col-lg-5 col-md-8", method: @form_method, remote: true) do |f|
    = f.alert_message t('errors.template.header', model: Seat.model_name.human, count: @seat.errors.count)
    = f.text_field :name, wrapper_class: "row", required: true

    %br
    = f.form_group do
      = f.submit class: "btn btn-success"
      %button.btn.btn-link.cancel= t("helpers.cancel")

:css
  .checkbox-inline {
    width: 13rem;
  }
